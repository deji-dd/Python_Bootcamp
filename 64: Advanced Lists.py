lst = [1, 2, 3, 4]
lst.insert(2, 'inserted')  # inserts an object in a list at a given index
lst.remove('inserted')  # removes the first instance of an object in a list
lst.reverse()  # reverses the list
lst.sort()  # sorts the list
