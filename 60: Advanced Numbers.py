print(hex(12))  # hex returns the hexadecimal of a number
print(hex(512))
print(bin(128))  # bin returns the binary representation of numbers
print(bin(512))
print(pow(2, 4))  # pow raises a number to a given power
print(abs(-3))  # abs returns the absolute value
print(round(3.5))  # rounds up to 0 decimal points
